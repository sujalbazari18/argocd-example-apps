apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui
spec:
  type: ClusterIP  # 👈 Changed from default ClusterIP to LoadBalancer
  selector:
    app: guestbook-ui
  ports:
    - port: 80         # Service port
      targetPort: 80   # Container port (inside Pod)
